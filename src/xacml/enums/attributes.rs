
#[allow(dead_code)] 
// During development it does not make sense to have dead code warngings here
#[derive(PartialEq, Eq)]
pub enum Attributes{
    CurrentTime,
    CurrentDate,
    CurrentDateTime
}